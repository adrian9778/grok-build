# 2026-08-25

## docs/sourceReader 4层规范版 续写任务

- 背景：`docs/adrian/sourceReader/` 已被重生成成「4层规范」格式（00-05 + README 已存在，遵循用户给的 style guide）。旧 06-16 + 可靠性说明书 在 git HEAD 仍可恢复（`git show HEAD:docs/adrian/sourceReader/<file>`），但已从工作区删除、未被新格式替换。
- SOURCE_REV 文件记录 `437c7c92...`（xai-org 上游提交），本地对象库不可达，无法自动 diff，只能逐 crate 核对。
- 已交付（新格式，已同步当前代码）：
  - `06-配置与数据流.md`：合并顺序以 `xai-grok-config/src/lib.rs:1-14` 为准；路径 `paths.rs`；env 覆盖 `env_overlay.rs`；4 MiB 上限。
  - `07-程序入口与运行模式.md`：组合根 `xai-grok-pager-bin/src/main.rs`；`Command` 枚举现 23 变体（`cli.rs:9-154`，比旧文档更宽）；`AgentCmd` `cli.rs:342-351`；`GROK_WORKER_THREADS` `main.rs:1627/1668`。
  - `08-TUI交互循环与渲染.md`：`dispatch::dispatch(Action,&mut AppView)->Vec<Effect>` `dispatch/router.rs:149`（同步可单测）；`effects::execute` `event_loop.rs:4496` spawn 进 `JoinSet<TaskResult>`；主循环 `tokio::select!` `event_loop.rs:2552`；`Action/Effect/TaskResult` 在 `actions.rs:40/1446/2333`；AppView `app_view.rs:616`；agent_view/ ~30 文件、acp_handler/ ~14 文件（比旧文档宽）。
  - `09-Agent会话与模型循环.md`：`spawn_session_actor` `spawn.rs:188`；`SessionHandle` `handle.rs:47`；`SessionLiveState` `handle.rs:23`；`SessionCommand` `commands.rs:249`（Prompt 变体 `run_loop.rs:547`）；`handle_prompt` `turn.rs:249`→`handle_turn_input` `turn.rs:292`；`ChatStateHandle::build_request` `xai-chat-state/handle.rs:361`；`run_turn_via_sampler` `sampler_turn.rs:1142`；`execute_tool_calls_batch` `tool_calls.rs:372`；`SamplerHandle` `xai-grok-sampler/handle.rs:19`；`SamplingEvent` `events.rs:52`（Completed 为提交屏障）；`PromptCompletionKind` `commands.rs:38`。
- 待续写（TaskCreate #19-#29）：10-工具, 11-Workspace, 12-认证网络, 13-持久化, 14-构建测试, 15-跨模块场景, 16-术语表, 可靠性与通用技术实现说明书（09 已完成）。
- 关键真实漂移（同步依据）：config 系统已演进出 signed_policy/campaigns/macos_managed/managed_cache/env_overlay/version_overrides；Command 命令面大幅扩张。
- 已交付续写：
  - `10-工具协议与扩展体系.md`：3 基座 crate (xai-tool-types/protocol/runtime)；`Tool` trait `tool.rs:36`、`run:101`/`execute:87`、`ToolDyn` `:306` blanket；`ToolDispatch` `dispatch.rs:32`(`call_terminal:50`)；`ToolSearchIndex` `search.rs:63` + `SearchSnapshot`/`ServerSummary`；`CompoundResolver` `xai-computer-hub-core/src/resolver.rs:215` + `InnerDispatchForResolver` `inner.rs:56`；`TypedToolOutput` `tool.rs:246`；`DEFAULT_TOOL_OUTPUT_BYTES=40_000` `xai-grok-tools/src/lib.rs:7`（截断在 `registry/types.rs:1171/1179`）；`PROTOCOL_VERSION="1.0.0"` `xai-tool-protocol/src/handshake.rs:10`；MCP `start_mcp_server` `servers.rs:4545`/`start_mcp_servers:4685`；Hooks `HookRegistry` `discovery.rs:16`/`load_hooks:245`/`run_hook` `runner/mod.rs:163`；Skills `SkillManager` `skill_discovery_tracker/mod.rs:85`/`SkillTool` `opencode/skill/mod.rs:77`。
  - `11-Workspace权限沙箱与Git.md`：`WorkspaceClient` `xai-grok-workspace-client/src/lib.rs:159` + git_* 全集(286-383)；权限双层 `xai-grok-workspace/src/permission/`（types.rs 枚举 AccessKind:200/Decision:219/EditPolicy:234/RuleAction:436/ToolFilter:445/RequirementSource:458/Sourced:505）；`PermissionManager` actor `manager/mod.rs:1404`(`spawn_permission_manager_with_hub:1442`)、`request:1057`/`request_with_path_context:1083`、`set_yolo_mode:880`/`is_yolo_mode:1021`/`set_auto_mode:905`；`gate_preflight.rs:1` 合并直接规则+两道 bash 安全门(exec_risk/bash_command_splitting)保留 Ask 溯源；`SandboxManager` `xai-grok-sandbox/src/lib.rs:159`(`apply:179`/`install:249`/`restrict_child_network:271`)，nono/Landlock/Seatbelt + 子进程 seccomp 封网、`support_info:263`。
- 已交付续写：
  - `12-认证网络遥测与更新.md`：认证 seam `xai-grok-auth`（依赖倒置：`HttpAuth` `visibility.rs:5`、`AuthCredentialProvider` `auth_provider.rs:39`、`CredentialSnapshot:14`、`StaticAuthCredentialProvider:78`、`AuthRetryMiddleware` `retry_middleware.rs:36`+`execute_with_stamp:27`）；壳层实现 `xai-grok-shell/src/auth/`（manager/flow/device_code/oidc/refresh/credential_provider/storage/jwt/single_flight）；Hub 认证 `xai-grok-workspace/src/hub_auth/mod.rs:385`(`provider`)+`default_auth_path:86`+`proactive.rs`；MCP OAuth `xai-grok-config-types/src/mcp.rs`（`McpJsonOAuthBlock:119`/`oauth_config:487`/`resolve_oauth_client_secret:19`）；遥测 `xai-grok-telemetry`（`TelemetryClient` `client.rs:48`、`TelemetryEvent` `events/mod.rs:17`、事件 `Login:376`/`PermissionOutcome:205`/`McpErrorType:294`/`InstallKind:354` + 技能 `SkillAdded/Removed/Dispatched`）；更新 `xai-grok-update/src/auto_update.rs`（`UpdateRunMode:25`/`check_update_background:600`/`run_update:2638`/`ensure_latest_on_disk:412`/`restart_grok:891`/`apply_channel_switch:2617`）。
- 已交付续写：
  - `13-持久化记忆与会话恢复.md`：记忆 `xai-grok-memory/src/lib.rs:1-17`(布局 `~/.grok/memory/` + blake3(cwd) 分桶) + `:21`(开关 GROK_MEMORY/`[memory] enabled`/远端) + `:55`(embed_missing_chunks)；壳层 shim `xai-grok-shell/src/session/memory/mod.rs:1-10`；会话 `xai-grok-shell/src/session`（`Summary` 向后兼容 head_commit/head_branch/pending_cwd_switch_reminder `persistence_head_fields_tests.rs`、`SessionInfo` `unified_list/row.rs:144`、`SessionMetaEnvelope` `envelope.rs:45`）；崩溃恢复 `xai-grok-active-sessions/src/lib.rs:1-3`(`ActiveSession:15`/`register:29`/`collect_crashed:40`/`collect_crashed_in:66`/`unregister_in:53`/`list_in:74`)；检索 `xai-grok-session-search/src/lib.rs:1-15`(SQLite FTS5 session_search.sqlite，分层 fts/recovery/db/doc/bootstrap/manager，SessionSource/ContentExtractor 注入)。
- 已交付续写：
  - `14-构建测试第三方与许可证.md`：工具链 `rust-toolchain.toml`(1.94.0)；proto 生成 `xai-proto-build/src/lib.rs:34`(`XaiProtoBuilder` + 配置方法 55-117)；build.rs 列表(shell/tools/tools-api/pager-bin/version)；测试支撑 `xai-grok-test-support/src/`(acp_client/headless/mock_server/counting_server/sandbox/leader/inference_override/env/process/resources) + `xai-grok-test-utils/src/git.rs:199`(write_fanout_tree)；第三方 vendoring `third_party/`(dagre_rust/mermaid-to-svg/graphlib_rust/ordered_hashmap + NOTICE/README)；许可证 `LICENSE`/`SECURITY.md`/`THIRD-PARTY-NOTICES`/`third_party/NOTICE`。
- 已交付续写：
  - `15-跨模块完整运行链与场景.md`：综合章，三幕场景(A 直答/B bash改文件/C 崩溃恢复)映射到 07-14 各模块符号；强调「同步 dispatch::dispatch `dispatch/router.rs:149` + 异步 effects::execute `event_loop.rs:4496` + tokio::select! `event_loop.rs:2552`」「会话单写者 Actor」「SamplingEvent::Completed 提交屏障」「ToolDispatch::call 统一入口」「权限+沙箱执行前横切」「fail-closed 一致」八条全系统不变量。
- 已交付续写：
  - `16-术语表与源码查找手册.md`：参考章，术语表(28 条)+源码查找手册(概念→crate→file:line)+grep 起手式；基准 SOURCE_REV 注明需复核。
- 待续写：`可靠性与通用技术实现说明书.md`(task #29) — 综合可靠性模式(Actor 单写者/fail-closed/Hub 重连重放/crash-handler/AuthRetryMiddleware stamp 重试/active-sessions 崩溃恢复/sandbox/ToolStream 不变量/SamplingEvent 提交屏障/session_search 重建)。
- **已完成（全 4 层 doc set 闭环）**：`docs/adrian/sourceReader/` 现含 00-16（11 章按 4 层规范新写：10-16 本日续写）+ README + 可靠性与通用技术实现说明书。README TOC 已一致引用 10-16 与可靠性说明书。所有 `file:line` 均对应当前工作区（SOURCE_REV 437c7c92）。
- 收尾核验（本回合）：00-05 + README 确认已在磁盘且为 4 层规范格式（00=143行/01=244/02=176/03=170/04=667/05=602；README TOC 00-05 条目齐全，导航行 00→01→…→05→06 链完整）。tasks #1-#6 因交付物已存在而标记完成，全 29 任务清零。
- 新方法：每章从 git HEAD 旧文取技术底稿 + 读当前对应 crate 源码核对符号 + 新4层风格（导航行 + `> 场景/阅读说明` 版本钉死 + ASCII/mermaid 分工 + `file:line` 索引）。

## 精校轮（precision pass + 逐函数拆解）：00-09

- 用户指令「做一轮精校，针对其它章节做更深的逐函数拆解」。对 00-09 做了 `Read`/`Grep` 逐锚点复核 + 对 06-09 加真实函数体骨架。
- 复核结论（00-05 + 07 + 08 + 09 锚点全部吻合，仅 06/09 有漂移，已修）：
  - 04：`SessionHandle:47/48/50/73`、`ChatStateHandle:20/21/40`、`SamplerHandle:19/20`、`WorkspaceHandle:431`、`PermissionHandle`(实为 `enum` `manager/mod.rs:78` 非 struct)、`SessionActor`(`pub(crate)` `acp_session.rs:645`)、`ChatStateActor:30/36/38/40`、`SamplerActor:27-42`、`ChatStateCommand:56`、`ChatStateEvent:8`、`SessionEvent`(`pub(crate)` `replay_events.rs:106`)、`SamplerCommand`/`SamplingEvent` 全 variant 行号 —— 全部 VERIFIED。
  - 05：`Command:9`/`PagerArgs:431`、`Action:40`/`Effect:1446`/`TaskResult:2333`、`acp_send` 调用点 `effects/mod.rs:1141`（定义 `acp_send_bounded` 在 `effects/helpers.rs:27`，re-export 成立）、`Tool:36`/`ToolDispatch:32`/`ToolCallContext:66`、`WorkspaceOps:1452`/`call_tool:1713`、`SamplerHandle:19`/`SamplingClient:357`/`SamplingEvent:52`、`ChatPersistence:23`、`HttpAuth:5`/`AuthCredentialProvider:39`、`PROTOCOL_VERSION:10`/`Method:23`/`handshake` —— 全部 VERIFIED。
  - 07：`main.rs` `main:1879`/`async_main:1970`/`match command:2042`（`args.command.take()` 后 `match command`，行号正确）/`run_agent_command:1099`/`shutdown_and_flush_telemetry:1058`/`cli_worker_threads:1668` —— VERIFIED。
  - 08：`event_loop.rs` `run:1046`/`select!:2552`/`process_effects:4488`/`execute:4496`、`dispatch/router.rs:149`、`app_view.rs:616`、`dispatch/mod.rs:10` —— VERIFIED。
  - 09：`handle_prompt:249`/`handle_turn_input:292`(含 `input_origin.policy():331`/`is_human_intent():338`)、`build_request:361`/`run_turn_via_sampler:1142`/`spawn_session_actor:188`/`SessionLiveState:23`/`PromptCompletionKind:38`/`run_loop.rs:547`/`prompt_queue.rs:28,38,748`/`turn_end_hooks.rs:133`/`attempt_runner.rs:134,159` —— VERIFIED（除下条）。
- **发现并修正的漂移**：
  - `09`：`execute_tool_calls_batch` 实为 `tool_calls.rs:494`（文档误写 `:372`）—— 4 处全部改为 `494`（§3 流程图/表、§5、§11）。
  - `06`：`loader.rs:55-62` 实为 TOML 解析错误的 `line_col` 辅助函数，非加载器；真实：常量 `USER/MANAGED/REQUIREMENTS_FILENAME` 在 `loader.rs:92-98`，`load_managed_config:100`/`load_system_managed_config:115`/`managed_config_layers:137`。两处引用已修正。
- **新增「逐函数骨架」小节（真实函数体，带行号）**：
  - `09` §3.1：`handle_prompt:249`→`handle_turn_input:292`→`run_turn_via_sampler:1142`(含 `submit_turn_request:1173`/`recover_from_sampling_failure:1223`)→`execute_tool_calls_batch:494` 四段 Rust 骨架 + 关键分支注释（is_human_intent 权威分支、限流退避重试、tool_result 闭合保证不变量3）。
  - `08` §4.1：`dispatch::dispatch:149`（纯同步 `match action -> Vec<Effect>`）+ `process_effects:4488`（spawn 进 JoinSet、安装 auth abort 句柄）两段 Rust 骨架。
- 结论：00-09 锚点精度达标；06/09 漂移已修；08/09 已满足「更深逐函数拆解」要求。10-16 + 可靠性说明书本日已写、本次未重校（如需可下一轮）。

## 精校轮（续）：10-16 + 可靠性说明书

- 对 10-16 + 可靠性与通用技术实现说明书 做 `Read`/`Grep` 逐锚点复核 + 对函数最密集的 11/12 章补真实函数体拆解。
- **已加「真实函数体拆解」小节**：
  - `11` §4.7：`SandboxManager::new/apply/install/restrict_child_network`（`sandbox/lib.rs:167/179/249/271`，含 `apply` 的 `#[cfg(enforce,unix)]` 不可逆内核路径 + 降级继续）+ `PermissionManager::request`/`request_with_path_context`（`manager/mod.rs:1057/1083`，两层薄壳，真正裁决在 `request_with_path_context_resolved:1110` 的 `match self` 发 `PermissionCommand::Request`，由 `gate_preflight` 合并）。
  - `12` §4.6：`AuthRetryMiddleware` —— `execute_with_stamp`（`retry_middleware.rs:27`，自由函数，把 stamp 记进 `Extensions`）+ `impl Middleware for AuthRetryMiddleware::handle`（`retry_middleware.rs:64-104`，真正的 401 刷新重发循环）。
- **复核 VERIFIED 正确的关键锚点（大量，举要）**：
  - `10`：tool.rs:36/87/101/306/337/358；dispatch.rs:32/50/64；search.rs:11/31/42/63/77；resolver.rs:215/248（CompoundResolver/resolve）；handshake.rs:10；servers.rs:4545/4685/316/3439；oauth.rs:81；credentials.rs:90；acp_transport.rs:74；liveness.rs:147；elicitation.rs:109；skills（SkillManager:85/SkillTool:77/SkillDiscoveryReminder:45/SkillInfo:41/SkillsClient:376）。
  - `11`：workspace-client git_* 全集 286-383 全中；permission/manager 1404/1442/1057/1083/1110/880/905/1021/1028/1048 全中；sandbox 159/167/179/241/249/271/90/96/124/128/136/155/263 全中；types.rs 8/123/131/200/219/234/274/279/389/406/418/428/436/445/458/505 全中（EditPolicy/PromptPolicy/PermissionCommand/ToolFilter 为 enum 但行号吻合）。
  - `12`：auth_provider 39/14/78；visibility 5；retry_middleware 21/27/36/42（handle 重试循环 :64-104）；hub_auth 385/86；update auto_update 25/164/193/235/364/412/506/565/571/600/685/891/1223/1283/1393/1531/2617/2638 全中；mcp.rs 19/119/487 全中；telemetry client.rs:48 / events/mod.rs:17/Login:376/SkillAdded:1127/SkillRemoved:1134/SkillDispatched:1151。
  - `13`：memory 21/55；active-sessions 15/29/35/40/46/53/66/74；session-search 模块声明 17-22（fts/recovery/db/doc/bootstrap/manager）；unified_list row.rs:144 / envelope.rs:45。
  - `14`：xai-proto-build 34/55/59/63/67/72/77/86/97/106/112/117 全中；TestSandbox 19。
  - `可靠性`：crash-handler lib.rs 3/6/15/46/56/81/101/122 全中。
- **发现并修正的漂移（本轮）**：
  1. `10` §4.3：`dispatch.rs:63` → `:64`（stream_no_terminal 实际行）。
  2. `10` §4.4：`registry/types.rs:1171/:1179` → `:1168/:1176`（`interpolate_description`/`apply_to_schema`）；`namespace/id :1183-1185` → `:1184`。
  3. `10` §4.6：`inner.rs:35` → `:34`（InnerDispatchForResolver struct）；`ResolvedTool`「near :26」→ `resolver.rs:34` enum / `ToolHandle:80` trait / `ErasedTool<T>:114` struct。
  4. `10` §4.9：`connection.rs` 路径错误（`HubConnection` 实际在 **xai-computer-hub-sdk**/src/connection.rs，非 xai-computer-hub-core）→ 改为 `xai-computer-hub-sdk/src/connection.rs:5-12`/`:14-25`（:479 struct / :622 impl）。
  5. `12` §4.4/§2：`InstallKind:354→:349`、`PermissionOutcome:205→:198`、`McpErrorType:294→:285`、`CliUpdateInstaller:143→:132`、`CliUpdateTrigger:173→:164`（统一指向 enum 声明行）。
  6. `14`：`xai-grok-test-utils`（错误 crate 名）→ `xai-test-utils`（真实 crate `crates/common/xai-test-utils`；`write_fanout_tree` 在 `git.rs:199` 正确）—— 4 处（blockquote/diagram/step4/§4.3）。
  7. `15`/`16`：`tool_calls.rs:372` → `:494`（stale drift B，此前只修了 09，未波及 10/15/16）→ 3 处。
  8. `05`（前轮漏改）：`dispatch.rs:63` → `:64`（§277/§302/§587 共 3 处）。
  9. `可靠性`：§92/§132 `dispatch.rs:63`→`:64`；§63/§134/§144 短路径 `connection.rs:*` → `xai-computer-hub-sdk/src/connection.rs:*`。
- 终检：全 docs 目录 grep `xai-grok-test-utils` / `dispatch.rs:63` / `tool_calls.rs:372` → 0 命中。10-16 + 可靠性说明书锚点精度达标；11/12 已满足「更深逐函数拆解」。
